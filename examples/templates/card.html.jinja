<div class="card">
  <div class="title">{{ title }}</div>
  <div class="subtitle">{{ subtitle }}</div>
</div>

<style>
.card {
  width: 1200px;
  height: 630px;
  display: flex;
  flex-direction: column;
  justify-content: center;
  padding: 64px;
  background: #111827;
  color: white;
}

.title {
  font-size: 72px;
  font-weight: 800;
}

.subtitle {
  margin-top: 24px;
  font-size: 36px;
  opacity: 0.8;
}
</style>
